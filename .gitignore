# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Runtime + local configuration: these hold real server addresses.
apollo.runtime.json
.env
.env.local
.env.*.local
!.env.example

# Agent worktrees live here. They are checkouts of this repo, not content of it —
# committing them adds gitlinks nobody can resolve, and they get swept up by any
# tool that globs the working tree.
.claude/worktrees/
